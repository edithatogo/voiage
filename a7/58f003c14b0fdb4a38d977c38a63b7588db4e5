Coverage repair for merged PR #707. Tests exercise every previously uncovered JSON Table error branch: suffix, decode, non-string key seam, Arrow conversion seam, invalid resource path, unsupported parser declarations, empty schema, and invalid field declaration. Validation: 149 provider and file-backed fixture tests passed; targeted JSON Table tests (12), Ruff, production ty, full Conductor, GitHub cross-reference, and diff checks passed. Local pytest-cov was unable to import NumPy under coverage instrumentation (cannot load module more than once); normal test execution is clean and hosted changed-code coverage remains the authority.
