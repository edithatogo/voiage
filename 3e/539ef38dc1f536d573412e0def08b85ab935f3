P4 Frictionless TSV local-profile increment. Adds only explicit Data Package TSV resources: .tsv path, format=tsv, and an exact tab delimiter. The adapter continues to reject inference, custom dialect properties, path/format mismatches, remote references, archives, transforms, and non-CSV/TSV formats. Updated provider capabilities and resource receipts expose text/tab-separated-values. Added valid and adversarial file-backed fixtures plus provider and CLI capability coverage. Validation on rebased head: uv run --locked --extra dev --extra ci pytest tests/test_standardized_ingestion_providers.py tests/test_frictionless_offline_fixtures.py tests/test_standardized_ingestion_cli.py -q (139 passed); Ruff check/format; ty check; Conductor full validation and GitHub cross-reference validation. Docs Astro build and dependency-frontier upgrade were not run to completion because /Volumes/PortableSSD had 109 MiB free and the latter failed extracting Polars with ENOSPC; uv.lock was restored without changes. This is incremental P4 evidence only and does not complete the active track or claim full Frictionless support.
