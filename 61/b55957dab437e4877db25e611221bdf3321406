EVPPI contract reconciliation: documented the default Rust-backed linear path and Python-only option boundary, updated the migration matrix, added EVPPI to the committed production benchmark workload, and fixed the profiling job's explicit stable Rust toolchain input. Validation: actionlint benchmark workflow, cargo bench --locked with EVPI/ENBS/EVPPI/dominance/CEAF output, benchmark contract check, git diff --check.
