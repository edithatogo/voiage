Migrates repository-managed dependency updates from Dependabot configuration to Renovate. Enables explicit Cargo, GitHub Actions, npm, PEP 621/uv, requirements, and setup.py managers; removes the Dependabot version-update manifest; updates the quality-gate assertion and public planning documentation. Validation: jq empty renovate.json; uv run --extra ci --extra dev pytest tests/test_ci_cd_quality_gates.py -q --no-cov (30 passed); uv run --extra dev tox -e vale (passed).
