Phase 3 Rust domain TDD slice: added validated DecisionProblem and Intervention aggregates plus typed Provenance, ExecutionMode, and ReproducibilityMetadata. Fail-closed serde rejects unknown fields and constructor-bypassing invalid states. Enforced nonempty unique intervention IDs, positive Threshold through the existing primitive, fixture identity in deterministic fixture mode, and seeds for stochastic execution. Files: rust/crates/voiage-domain/src/lib.rs; rust/crates/voiage-domain/src/decision_problem.rs; rust/crates/voiage-domain/tests/decision_problem.rs. Red evidence: cargo test -p voiage-domain --test decision_problem --locked failed with unresolved aggregate imports. Green evidence: cargo fmt -p voiage-domain -- --check; cargo clippy -p voiage-domain --all-targets --locked -- -D warnings; cargo test -p voiage-domain --all-targets --locked (15 passed); cargo test -p voiage-domain --doc --locked; git diff --check -- rust/crates/voiage-domain.
