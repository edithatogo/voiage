Phase 7 adapter conversion: added the R-safe voiage_v1_evpi_i32_r C ABI wrapper with pointer-width-safe dimensions and explicit status output; routed R evpi() directly through the Rust core while preserving injectable compatibility tests and population scaling; wired R CI to build the ABI. Validation: Rust ffi tests/clippy/release build passed; direct R .C smoke passed with EVPI 3.0 and status 0. Full R CMD check remains externally limited because local reticulate/testthat are not installed.
