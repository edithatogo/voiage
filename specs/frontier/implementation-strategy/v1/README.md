# Implementation-Strategy Fixtures

This directory defines the fixture-backed contract for comparing alternative
implementation strategies through uptake, adherence, coverage, delay, scale-up
cost, and population impact. The Python runtime and CLI are fixture-backed;
cross-language parity and mature promotion remain gated.

Inputs use `samples x strategies x periods` net benefits and `periods x
strategies` implementation matrices.
