# Frontier Fixture Registry

This directory contains the registry manifest for the experimental frontier
fixture-backed contracts.

The registry exists so downstream validation can discover every committed
frontier fixture family from one location rather than inferring it from the
directory tree.
