# Preference Fixture-Backed Contract

This directory contains the deterministic fixture set for the preference
heterogeneity and individualized-care frontier contract.

The normative payloads anchor the `sample x strategy x preference_profile`
surface so the runtime, CLI, and downstream bindings can validate against the
same exact JSON object.
