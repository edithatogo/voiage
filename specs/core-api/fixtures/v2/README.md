# Core API v2 fixtures

`normalized-input-bundle.json` is the deterministic, normative manifest fixture
for the normalized input contract. It pins the canonical JSON representation
and SHA-256 digest alongside a schema-valid payload. Format providers and
conformance suites may use it without importing an external parser.
