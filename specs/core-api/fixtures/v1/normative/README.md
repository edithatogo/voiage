# Normative Fixtures

Place deterministic, contract-bearing fixtures here.

These fixtures are the canonical outputs for conformance checks and should remain stable across runs.

The shared deterministic input bundle lives under `normative/inputs/` and is paired with each canonical output case through the manifest.
