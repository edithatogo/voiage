Task: Decision Problem JSON C ABI transport. Added ABI v1.10 with fail-closed validation through the stable Rust DecisionProblem type, normalized compact JSON, query-then-copy caller ownership, trailing NUL, explicit serialization failures, panic containment, and no partial writes for invalid input or short buffers. Added serialization and FFI contract tests plus strict C/C++/sanitizer consumers, capability/version/symbol synchronization, and Astro/roadmap/todo/changelog documentation. This is normalized transport JSON, not RFC 8785 digest canonicalization; result DTO JSON, Arrow, and language-native adapters remain open. Serialization and FFI package tests, warnings-denied Clippy, strict C/C++, 40 focused Python governance tests, Astro, repository harness, and diff checks passed.
