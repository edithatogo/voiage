# Historical Arrow 25.0.0 solver evidence

These files preserve exact manifest, receipt and solver-log bytes from signed
commit `38b573b5ae7fa7b9afd907d586ced7cafd9a8de2`. Log and concrete-DAG paths
resolve relative to this directory. The original prior-evidence path resolves
from the overlay root; recipe paths refer to that Git commit, not current files.
The record proves the earlier macOS solve, not a native build or replay.
