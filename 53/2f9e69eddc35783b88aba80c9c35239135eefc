Task: Address repository-local R/HPC readiness checks. validate_r_ffi_install.sh passed end-to-end (Rust FFI build, temporary R install, installed EVPI smoke). validate_hpc_recipes.sh now isolates Spack configuration and fails closed when EasyBuild lacks modules. pkgcheck installs in a temporary r-universe library and remains in progress. No external submission or registry action.
