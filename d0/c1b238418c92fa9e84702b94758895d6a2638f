P4-T7 partial progress: added file-backed Frictionless Data Package v1 fixtures for a valid explicit-schema CSV package and fail-closed multiple-resource, dialect, non-CSV-format, integrity-declaration, type, required-null, and primary-key cases. Tightened the built-in provider to reject declared resource formats other than csv and integrity declarations it cannot verify. Validation: uv run --extra ci --extra dev pytest tests/test_frictionless_offline_fixtures.py tests/test_standardized_ingestion_providers.py -q --no-cov (75 passed); uv run --extra ci --extra dev ruff check voiage/ingestion/frictionless.py tests/test_frictionless_offline_fixtures.py (passed).
