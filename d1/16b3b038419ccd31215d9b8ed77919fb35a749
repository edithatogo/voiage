rOpenSci readiness classification: verified the official author guidance for unusual system dependencies. It directs package authors to the external rOpenSci checking-image route when needed. The evidence matrix now represents the voiage-ffi distribution/checking-environment dependency as external rather than incorrectly suggesting a local repository blocker; local FFI install and pkgcheck evidence remain separate. Validation: uv run pytest tests/test_submission_readiness_contract.py --no-cov (8 passed); uv run python scripts/validate_submission_readiness.py; uv run vale docs/release/ropensci-local-validation.md docs/release/ropensci-system-dependency-gate.md; git diff --check.
