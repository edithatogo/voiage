PR #669 refresh evidence: rebased onto origin/main 5a5ddcf8 (2026-07-31). Preserves cross-provider generated mapping properties and adversarial malformed-node/digest mismatch cases. Validation: uv run --extra dev pytest -q tests/test_standardized_ingestion_conformance.py tests/test_standardized_ingestion_providers.py (97 passed); uv run --with tox tox -e ingestion-conformance (passed); Ruff targeted checks (passed); full Conductor validator and GitHub cross-reference validator (valid); git diff --check (passed). This note records local evidence only; protected hosted checks remain required before merge.
