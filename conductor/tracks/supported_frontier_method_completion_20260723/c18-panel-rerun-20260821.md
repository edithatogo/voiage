# C18 M22–M31 panel rerun — exact merged baseline

This is a candidate-bound advisory panel assessment. It records the panel
recommendation and the maintainer's stated authorization boundary; it is not an
external publication, registry, or editorial receipt.

## Candidate

- Exact `main` commit: `67dca55b245779e6d5d0f9f7bb638a59e93fc674`
- PR #989: merged after all required checks passed.
- Scope: M22–M31 semantics freezes, executable evidence, hash-pinned
  pathological fixtures, and the prior panel dissent.

## Separated panel reports

The orchestrator used four role-separated advisory reports:

1. **Estimand/domain:** the finite contracts are coherent within their declared
   scopes; limitations remain for DVSS, continuous density, general POMDPs,
   welfare comparability, and assumption-scoped outcome identities.
2. **Numerical/evidence assurance:** normative fixtures and executable
   pathology/property references are now hash-bound. Empirical estimator
   operating-characteristic evidence and broad randomized pathology corpora are
   still absent.
3. **API/parity:** Python is the exact executable baseline. Rust, R, and Julia
   promotion parity has not been demonstrated and must be recorded as
   unsupported/unverified until shared-fixture runs pass.
4. **Governance/promotion:** all families remain experimental; no panel output
   can authorize stable promotion, release, publication, registry acceptance,
   or issue closure.

## Options and recommendation

- **A — conditional experimental acceptance (recommended):** accept the
  repository-owned contracts and fixtures as experimental evidence, preserve
  all methods as experimental, and run parity against this exact Python head.
- **B — staged promotion:** promote only families with complete scientific,
  fixture, and cross-language evidence. This risks inconsistent maturity and is
  not recommended yet.
- **C — full deferral:** defer parity and all promotion until empirical and
  cross-language evidence is complete. This is the fallback if any semantic
  disagreement becomes blocking.

The panel recommends **Option A**. Dissent is preserved from the prior rerun;
it is not resolved by majority vote.

## Accountable decision boundary

The repository owner has stated that they are the accountable scientist,
maintainer, and release approver. That authorization permits repository-owned
promotion/release decisions to be considered after the panel recommendation,
but it does not create external curator, editor, registry, or community
evidence. No stable claim or external submission is inferred by this packet.

## Next executable gate

Run exact shared-fixture parity for Python, Rust, R, and Julia. Record each
binding as verified only when its installed artifact matches the Python
semantics; otherwise record an explicit unsupported/unverified receipt. Keep
all methods experimental if parity or any scientific condition remains open.
