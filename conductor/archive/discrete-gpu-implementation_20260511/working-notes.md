# Working Notes: Discrete GPU Implementation

The discrete GPU track is the hardware-scale follow-on after Apple Metal.
The CPU fallback remains the reference path and the shared abstraction contract
must stay intact.
