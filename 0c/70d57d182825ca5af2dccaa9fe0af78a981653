Task: Integrate portable v1 lifecycle exports

Summary:
- export namespaced u64 token create/free functions from the FFI crate root
- use the shared stable VoiageStatusV1 registry for both lifecycle operations
- validate caller-owned create output pointers and contain panics at both C boundaries
- preserve null-free, stale-token rejection, and thread-safe lifecycle behavior

Files:
- rust/crates/voiage-ffi/src/lib.rs
- rust/crates/voiage-ffi/src/lifecycle.rs
- rust/crates/voiage-ffi/tests/lifecycle.rs

Verification:
- cargo fmt --all --check
- cargo clippy -p voiage-ffi --all-targets --locked -- -D warnings
- cargo test -p voiage-ffi --locked (18 tests passed)
- C11 and C++17 header smoke compilation
- exported symbols exactly matched specs/abi/v1/symbols.txt
- git diff --check
