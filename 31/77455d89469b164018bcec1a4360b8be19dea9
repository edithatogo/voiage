Task: P5-T6 local matrix acceleration. Summary: Align Python 3.12-3.14 and minimum/maximum dependency tox lanes with the hosted compatibility contract set while retaining the complete parallel coverage environment; add a regression contract. Files: tox.ini; tests/test_ci_optimization_design.py. Why: The prior matrix repeated roughly 4,400 tests five times before running the same full suite for authoritative coverage.
