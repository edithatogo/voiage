Task: stable regression EVPPI C ABI. Added ABI v1.8 with an explicit full-sample linear-regression EVPPI entry point, independent net-benefit and parameter row counts, fixed-width fit dimensions, explicit incomplete-assurance state, diagnostic status mapping, panic containment, and no-write failures. Synchronized Rust exports, C header, capabilities, symbols, layouts, C/C++ and sanitizer consumers, Astro reference documentation, roadmap, todo, and changelog. Focused Rust tests and Clippy, strict C/C++ syntax checks, 40 Python ABI/governance tests, Astro checks, repository harness, and diff checks passed. A full tox run passed immediately before this FFI-only slice; EVSI, serialization, language-native adapters, and replication/convergence assurance remain open.
