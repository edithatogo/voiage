Task: Remove the tomli backport and retire the stale Safety/pip-tools tox lane.\n\nSummary: Use stdlib tomllib across the supported Python 3.12-3.14 range, make pip-audit plus SBOM evidence authoritative for dependency scanning, and modernize final_validation.py to active controls.\n\nValidation: uv lock --upgrade; uv run python scripts/dependency_frontier.py . --strict; uv run pytest tests/test_quality_tool_consolidation.py --no-cov -q; uv run ruff check final_validation.py; uv run python final_validation.py; uv run tox (all configured lanes passed; 91.07% coverage).
