P7-T3 SourceAccessPolicy increment: stream built-in local CSV/TSV through Arrow record batches, reject configured resource and batch row ceilings before retaining batches or constructing a table, and expose --max-resource-rows on materializing ingest commands. Exact-head local validation: 241 focused ingestion/conformance/fixture/reference/benchmark/cache/SDK tests passed; Ruff, ty, Vale, full Conductor, cross-reference, evidence-chain, and diff checks passed. Remote/archive streaming remains unsupported or externally gated.
