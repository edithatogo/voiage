Task: EVPI v1 result-envelope JSON C ABI transport. Added ABI v1.11 with schema-validated EVPI normalization, caller-owned query/copy, trailing NUL, explicit serialization failures, panic containment, and no partial writes. Validation enforces the v1 discriminator, positive willingness-to-pay, finite values, non-negative EVPI, unknown-field rejection, and paired aligned optional strategy arrays. Synchronized header, capability/version/symbol contracts, Rust/C/C++/sanitizer consumers, and Astro/roadmap/todo/changelog documentation. Other result families and estimator-assurance JSON remain open. Serialization and FFI package tests, warnings-denied Clippy, strict C/C++, 40 focused Python governance tests, Astro, repository harness, and diff checks passed.
